# FORM is not known by linguist, so we manually override the language identifications.
# FORTRAN is the best approximation to the syntax colouring, so we stick with it.

*.frm linguist-language=Fortran
*.prc linguist-language=Fortran
*.hf linguist-language=Fortran
